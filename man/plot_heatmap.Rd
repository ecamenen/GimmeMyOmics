% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_bulk.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Plot Heatmap of Gene Expression Data}
\usage{
plot_heatmap(
  x,
  metadata_samples,
  count_normalized,
  transpose = FALSE,
  colour = colorRampPalette(brewer.pal(9, "Reds"))(255),
  ...
)
}
\arguments{
\item{x}{A \code{data.frame} or \code{tibble} containing gene identifiers in the first column.}

\item{metadata_samples}{A \code{data.frame} or \code{tibble} containing metadata for the samples.}

\item{count_normalized}{A matrix or \code{SummarizedExperiment} object containing normalized gene expression counts.}

\item{transpose}{Logical, whether to transpose the heatmap.}

\item{colour}{A color palette used for the heatmap.}

\item{...}{Additional arguments passed to \code{pheatmap}.}
}
\value{
A heatmap visualization of gene expression.
}
\description{
Plots a heatmap of gene expression data from normalized counts, with optional transposition and clustering.
}
\details{
The function performs the following steps:
\itemize{
\item Extracts expression data using gene identifiers from \code{x}.
\item Sets row names to gene names and column names to sample identifiers.
\item Computes sample and gene distance matrices for clustering.
\item Plots a heatmap using the \code{pheatmap} package.
}
}
\examples{
# Example usage
library(SummarizedExperiment)
library(S4Vectors)

# Create example counts matrix
nrows <- 5
ncols <- 3
counts <- matrix(runif(nrows * ncols, 1, 1e4), nrows, ncols)
gene_name <- c("TP53", "BRCA1", "EGFR", "BRAF", "KRAS")

# Create SummarizedExperiment object
count_normalized <- SummarizedExperiment(
    assays = SimpleList(counts = counts)
)
rownames(count_normalized) <- gene_name

# Example DEA results
dea_results <- data.frame(
    gene_name = gene_name,
    log2FoldChange = c(2.0, -1.8, 0.5, -0.2, 3.0),
    padj = c(0.001, 0.01, 0.1, 0.06, 0.0001)
)

# Metadata for samples
metadata_samples <- data.frame(sample_id = paste0("Sample", 1:ncols))

# Select top genes and plot heatmap
plot_heatmap(top_genes(dea_results), metadata_samples, count_normalized)

# Transposed heatmap
plot_heatmap(top_genes(dea_results), metadata_samples, count_normalized, transpose = TRUE)

}
