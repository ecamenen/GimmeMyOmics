% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_bulk.R
\name{format_dea}
\alias{format_dea}
\title{Format Differential Expression Analysis (DEA) Results}
\usage{
format_dea(
  x,
  metadata_genes = NULL,
  fc_threshold = 1,
  p_threshold = 0.05,
  name_id = "ensembl_gene_id"
)
}
\arguments{
\item{x}{A \code{data.frame} or \code{tibble} containing DEA results. Expected columns include \code{log2FoldChange}, \code{padj}, and row names representing Ensembl gene identifiers.}

\item{metadata_genes}{A \code{data.frame} containing gene metadata, including the columns \code{ensembl_gene_id} and \code{gene_name}.}

\item{fc_threshold}{Numeric, the absolute log2 fold change threshold. Default is \code{log2(2)}.}

\item{p_threshold}{Numeric, the adjusted p-value threshold. Default is \code{0.05}.}

\item{name_id}{A character string specifying the name of the column containing gene IDs. If \code{metadata_genes} is not \code{NULL}, this column name must also exist in \code{metadata_genes}.}
}
\value{
A \code{tibble} with the following additional columns:
\describe{
\item{log10p}{Numeric, the -log10 of the adjusted p-value.}
\item{gene_name}{Character, the official gene name from HGNC.}
\item{Expression}{Factor, the expression category ("Up-regulated," "Down-regulated," or "ns").}
}
}
\description{
Formats the results of a differential expression analysis (DEA) by adding relevant columns, filtering, and categorizing genes based on fold change and adjusted p-value thresholds.
}
\details{
The function performs the following steps:
\enumerate{
\item Adds a \code{log10p} column, which is the -log10 of the adjusted p-value (\code{padj}).
\item Merges gene metadata to include gene names.
\item Categorizes genes into "Up-regulated," "Down-regulated," or "ns" (not significant) based on the provided fold change and p-value thresholds.
\item Filters out genes with very small fold changes (absolute \code{log2FoldChange} < 0.01).
}
}
\examples{
# Example DEA results
ensembl_gene_id <- c("ENSG00000141510", "ENSG00000012048", "ENSG00000146648",
 "ENSG00000157764", "ENSG00000133703")
dea_results <- data.frame(
  log2FoldChange = c(2.0, -1.8, 0.5, -0.2, 3.0),
  padj = c(0.001, 0.01, 0.1, 0.06, 0.0001)
)
rownames(dea_results) <- ensembl_gene_id

# Example gene metadata
metadata_genes <- data.frame(
  ensembl_gene_id = ensembl_gene_id,
  gene_name = c("TP53", "BRCA1", "EGFR", "BRAF", "KRAS")
)

# Format DEA results
format_dea(dea_results, metadata_genes)

# Custom thresholds
format_dea(dea_results, metadata_genes, fc_threshold = log2(1.5), p_threshold = 0.01)
}
