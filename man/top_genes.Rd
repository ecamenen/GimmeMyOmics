% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_bulk.R
\name{top_genes}
\alias{top_genes}
\title{Filter Top Differentially Expressed Genes}
\usage{
top_genes(
  x,
  fc_threshold = 1,
  p_threshold = 0.05,
  n = Inf,
  return_rank = FALSE,
  rank_by = "rank_pfc",
  f = identity
)
}
\arguments{
\item{x}{A \code{data.frame} or \code{tibble} containing DEA results. Must include \code{log2FoldChange} and \code{padj} columns.}

\item{fc_threshold}{Numeric, minimum absolute log2 fold change required for inclusion.}

\item{p_threshold}{Numeric, maximum adjusted p-value allowed.}

\item{n}{Integer, number of top genes to return. Default is \code{1000}.}

\item{return_rank}{Logical, whether to return ranking columns (\code{rank_p}, \code{rank_fc}, \code{rank_pfc}).}

\item{rank_by}{Character, ranking variable.}

\item{f}{Function, sorting function (e.g., \code{desc} for descending).}
}
\value{
A \code{tibble} containing selected genes with additional computed ranking columns if \code{return_rank = TRUE}.
}
\description{
Filters the top differentially expressed genes from a DEA result set based on fold change, adjusted p-value, and a combined ranking metric.
}
\details{
The function applies the following steps:
\itemize{
\item Computes \code{pfc}, a ranking metric defined as \code{-log10(padj) * log2FoldChange}.
\item Calculates separate rankings for significance (\code{rank_p} from \code{log10(padj)}) and effect size (\code{rank_fc} from \code{abs(log2FoldChange)}).
\item Computes an average rank (\code{rank_pfc}) combining both ranks.
\item Filters genes based on the given fold change and p-value thresholds.
\item Selects the top \code{n} genes sorted by the specified ranking variable.
}
}
\examples{
# Example DEA results
dea_results <- data.frame(
  gene_name = c("TP53", "BRCA1", "EGFR", "BRAF", "KRAS"),
  log2FoldChange = c(2.0, -1.8, 0.5, -0.2, 3.0),
  padj = c(0.001, 0.01, 0.1, 0.06, 0.0001)
)

# Select top genes
top_genes(dea_results)

# Custom thresholds and return rankings
top_genes(dea_results, fc_threshold = log2(1.5), p_threshold = 0.01, 
return_rank = TRUE, rank_by = "log2FoldChange")

}
