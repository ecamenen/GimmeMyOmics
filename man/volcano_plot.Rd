% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_bulk.R
\name{volcano_plot}
\alias{volcano_plot}
\title{Volcano Plot for Differential Expression Analysis (DEA) Results}
\usage{
volcano_plot(
  x,
  top_genes = NULL,
  title = NULL,
  legend = "right",
  cex = 1.5,
  fc_threshold = 0.5,
  p_threshold = 0.05,
  force = 10,
  fc_log = 2,
  width = 30,
  breaks_x = NULL,
  breaks_y = NULL,
  fontface = "italic",
  ...
)
}
\arguments{
\item{x}{A \code{data.frame} or \code{tibble} containing DEA results. Must include \code{log2FoldChange}, \code{padj}, \code{gene_name}, and \code{Expression} columns.}

\item{top_genes}{Optional. A subset of \code{x} containing the most significant genes to be labeled on the plot.}

\item{title}{Optional. A character string specifying the title of the plot.}

\item{legend}{Character, position of the legend. Default is \code{"right"}. Other options include \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"none"}.}

\item{cex}{Numeric, scaling factor for text and points.}

\item{fc_threshold}{Numeric, absolute log2 fold change threshold to determine significance.}

\item{p_threshold}{Numeric, adjusted p-value threshold to determine significance.}

\item{force}{Numeric, force applied for label repulsion in \code{geom_text_repel()}.}

\item{fc_log}{Numeric, base for log transformation of fold change axis. If \code{0}, no transformation is applied.}

\item{width}{Numeric, width for wrapping gene labels in \code{geom_text_repel()}.}

\item{breaks_x}{Optional. Numeric vector of breakpoints for the x-axis (fold change). If \code{NULL}, it is automatically computed.}

\item{breaks_y}{Optional. Numeric vector of breakpoints for the y-axis (adjusted p-value). If \code{NULL}, it is automatically computed.}

\item{fontface}{Character, font style for gene labels (\code{"plain"}, \code{"italic"}, \code{"bold"}, etc.).}

\item{...}{Additional arguments passed to \code{geom_text_repel()}.}
}
\value{
A \code{ggplot2} object representing the volcano plot.
}
\description{
Generates a volcano plot to visualize differentially expressed genes based on log fold change and adjusted p-value.
}
\details{
The volcano plot represents genes based on:
\itemize{
\item \strong{x-axis (Fold change):} Log-transformed fold change (can be in log base 2 or another specified base).
\item \strong{y-axis (False Discovery Rate - FDR):} Adjusted p-value in a -log10 scale.
\item \strong{Color coding:} Up-regulated (red), down-regulated (blue), and non-significant (gray) genes.
\item \strong{Dashed lines:} Represent fold change (\code{fc_threshold}) and p-value (\code{p_threshold}) thresholds.
}

If \code{top_genes} is provided, labels will be added for those genes.
}
\examples{
# Example DEA results
dea_results <- data.frame(
  gene_name = c("TP53", "BRCA1", "EGFR", "BRAF", "KRAS"),
  log2FoldChange = c(2.0, -1.8, 0.5, -0.2, 3.0),
  padj = c(0.001, 0.01, 0.1, 0.06, 0.0001),
  Expression = c("Up-regulated", "Down-regulated", "ns", "ns", "Up-regulated")
)

# Generate volcano plot
volcano_plot(dea_results)

# Highlight top genes
tops <- top_genes(dea_results)
volcano_plot(dea_results, top_genes = tops)

}
